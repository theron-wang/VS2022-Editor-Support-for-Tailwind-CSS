name: Tests

on:
  push:
    branches:
      - '**'
  pull_request:

jobs:
  run-tests:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: |
            8.0.x
            10.0.x

      - name: Run tests
        run: dotnet test tests/TailwindExtension.Tests/TailwindExtension.Tests.csproj --configuration Release --verbosity normal
